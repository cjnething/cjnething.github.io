# Portfolio

Personal research portfolio hosted on GitHub Pages.

To preview locally, open `index.html` in a browser or run a simple static server:

```bash
python3 -m http.server 8000
```

Then visit http://localhost:8000
